# Running it

This page is section 10 of {doc}`the project record <PROJECT>`, included verbatim so it cannot fall out of step with
the record. Commands are written for Windows paths from the repository root; on Linux and macOS
use `.venv/bin/python`.

```{include} PROJECT.md
:start-after: "## 10. Running it"
```
